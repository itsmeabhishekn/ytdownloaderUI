export const SERVER_IP = import.meta.env.VITE_SERVER_IP || "http://localhost";
export const SERVER_PORT = import.meta.env.VITE_SERVER_PORT || "8000";
export const API_URL = `${SERVER_IP}:${SERVER_PORT}`;
